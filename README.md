# TaskFlow

TaskFlow is a Hebrew-first offline desktop task manager for a single local user.

## Run

```powershell
npm install
npm start
```

## Package

```powershell
npm run dist:win
```

The SQLite database is created automatically in Electron's user data directory on first launch.
